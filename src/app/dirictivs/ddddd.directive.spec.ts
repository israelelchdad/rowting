import { DddddDirective } from './ddddd.directive';

describe('DddddDirective', () => {
  it('should create an instance', () => {
    const directive = new DddddDirective();
    expect(directive).toBeTruthy();
  });
});
